# TSE Demo Audio

Each sample directory contains the input mixture, target reference, positive and negative enrollment waveforms, the TSE estimate, and metrics.yaml. WAV files use IEEE float samples without amplitude normalization. summary_metrics.yaml averages only these demo items; it is not the 5,000-item validation result.
